import { createRectangle, drawRectangle } from "."

const create = (tool) => {
    if (tool === "reactangle") {
        // createRectangle();
    }
}

const draw = (tool) => {
    if (tool === "reactangle") {

    }
}


export {draw, create};