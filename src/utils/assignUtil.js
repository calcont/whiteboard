import { createRectangle, drawRectangle } from "."

const create = (tool) => {
    if (tool === "reactangle") {
        console.log("createRectangle");
        // createRectangle();
    }
}

const draw = (tool) => {
    if (tool === "rectangle") {

        console.log("createRectangle");
    }
}


export {draw, create};